## write iso linux to usb

    # list mountet devices
    df

    # find and unmount usb
    sudo umount /dev/sdd1 

    # write iso to usb
    sudo dd if=ubuntu-19.04-desktop-amd64.iso of=/dev/sdd1 bs=1M status=progress 
